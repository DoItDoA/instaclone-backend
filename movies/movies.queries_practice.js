// 여기는 연습파트
/*
import client from "../client";

const queries = {
  Query: {
    movies: () => client.movie.findMany(),
    movie: (_, { id }) => client.movie.findUnique({ where: { id } }),
  }, // 클라이언트를 통해서 데이터베이스의 내용 가져옴
};
export default queries;
*/
